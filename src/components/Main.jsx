export default function Main() {
    return (
        <main className="main">
            <input type="text" placeholder="e.g. carrots" className="main--input" />
            <button className="add--ingredient-button">+ Add ingredient</button>

        </main>
    )
}